[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "flow-twinx"
version = "0.7.1"
description = "A terminal music player with online and offline modes sourced from youtube."
readme = "README.md"
requires-python = ">=3.14"
dynamic = []
dependencies = [
    "dbus-fast>=5.0.22",
    "dbus-python>=1.4.0",
    "flask",
    "numpy",
    "psutil>=7.2.2",
    "pygobject>=3.58.0",
    "python-vlc",
    "questionary",
    "sounddevice",
    "textual>=8.2.8",
    "yt-dlp>=2026.8.19",
    "ytmusicapi",
]

[project.scripts]
flow = "cli.main:main"
flow-tui = "tui:main"
flow-web = "web.main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["cli*", "backend*", "tui*", "web*"]

[tool.setuptools.package-data]
flow_twinx = ["Hyprland/*.qml"]
backend = ["web/templates/*"]

[dependency-groups]
dev = [
    "pyinstaller>=6.21.0",
    "textual-dev>=1.8.0",
]
